overwrite: true
schema: "./src/v1/graphqlUserSchema"
documents: null
generates:
  src/v1/graphqlUserSchema/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
