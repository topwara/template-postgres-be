overwrite: true
schema: "./src/v1/graphqlNonAuthSchema"
documents: null
generates:
  src/v1/graphqlNonAuthSchema/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
