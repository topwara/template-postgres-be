overwrite: true
schema: "./src/v1/graphqlAdminSchema"
documents: null
generates:
  src/v1/graphqlAdminSchema/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
